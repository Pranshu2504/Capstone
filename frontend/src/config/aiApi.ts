/**
 * Base URL of the try-on API for the native build.
 *
 * Try-on is served by the same process as the wardrobe API, so this matches
 * `@/config/api`. On the Android emulator, 10.0.2.2 is the host's localhost.
 */
export { API_BASE_URL as AI_BASE_URL } from './api';
